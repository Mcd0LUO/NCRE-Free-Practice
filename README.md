# NCRE 数据库刷题（三级 / 四级）

Node + Vite + React + Tailwind 的本地刷题应用。数据来自本机已安装的
「白菜课堂计算机等级考试学习系统」题库（该公司已停止运营），仅供个人学习。

## 启动

```bash
cd ncre-app
npm install          # 首次
npm run dev          # 同时起后端 + 前端
```

- 前端（Vite）：http://127.0.0.1:5180
- 后端（Express）：http://127.0.0.1:5181

Vite 已把 `/api` 与 `/images` 代理到后端，所以只需访问前端 5180。

单起：`npm run dev:server` / `npm run dev:web`
生产构建：`npm run build`（产物在 `dist/`，后端会自动托管它）

## 功能

| 模块 | 说明 |
|---|---|
| **分类练习** | 按「大类 → 知识点」两级分类进入，可再按题型过滤；每类显示进度与正确率 |
| **模拟考试** | 选考次 + 平行卷，限时倒计时（三级 120 分 / 四级 90 分），交卷统计 |
| **错题本** | 答错的题自动汇总，专项重刷 |
| **自动判分** | 单选、多选、填空全自动；多选支持部分分；主观题自评 |
| **进度持久化** | 服务端 `server/progress.json`，换浏览器不丢 |
| **键盘操作** | `A`-`E` / `1`-`5` 选项，`Enter` 确认或下一题，`←` `→` 翻页 |

## 分类体系

分类**不是猜的**，直接来自原库 `paper` 表的 `partid` / `sectionid`，
每题恰好归属 1 个分类（已校验：0 歧义，100% 覆盖）。

**三级数据库技术**（1680 题）—— 3 个大类 × 10 个知识点：

| 大类 | 知识点 |
|---|---|
| 选择题 | 数据库分析与设计 · UML与数据库应用系统 · 高级查询 · 数据库后台编程技术 · 数据库及数据库对象 · 安全管理 · 运行维护与优化 · 故障管理、备份与恢复 · 大规模数据库架构 · 数据仓库与数据挖掘 |
| 填空题 | 同上 10 个 |
| 设计与应用题 | 数据库分析与设计 · 数据库后台编程技术 · 运行维护与优化 |

**四级数据库工程师**（2746 题）—— 4 个大类 × 6 个知识点：

| 大类 | 知识点 |
|---|---|
| 操作系统原理-单选题 | 操作系统概述与运行机制 · 进程线程模型 · 同步机制与通信机制 · 存储管理 · 文件系统 · I/O设备管理与死锁 |
| 操作系统原理-多选题 | 同上 6 个 |
| 数据库原理-单选题 | 数据库系统基本概念和数据模型 · 关系数据模型和关系代数 · 关系数据库语言SQL · 关系数据理论和数据库设计 · 数据库管理系统 · 数据库技术发展 |
| 数据库原理-多选题 | 同上 6 个 |

## API

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/banks` | 题库列表 + 分类树 |
| GET | `/api/questions?bank=&part=&sec=&kind=&q=&page=&size=` | 分页筛选题目 |
| GET | `/api/stats/:bank` | 总进度 + 每分类正确率 |
| GET | `/api/wrong/:bank` | 错题列表 |
| GET | `/api/papers/:bank` | 试卷列表 |
| GET | `/api/paper/:bank/:ver/:group` | 单套试卷（含题目） |
| POST | `/api/answer` | 记录作答 |
| POST | `/api/mark` | 收藏/标记 |
| POST | `/api/reset` | 清空进度（`scope: all \| bank`） |

## 判分规则

- **单选**：字母完全一致
- **多选**：字母集合完全一致得满分；有对无错且库里有部分分规则时得部分分（四级 430 题有）
- **填空**：逐空判定，忽略大小写/空格/全角括号，`mark` 里的多个备选答案任一命中即算对；
  得分 = 满分 × 答对空数 / 总空数
- **设计与应用题**：主观题，自评

判分逻辑在 `src/grading.js`，有 18 条单元测试：`node test-grading.mjs`

## 数据结构

`server/data/banks.json`（4.2MB）由题库导出脚本生成：

```jsonc
{
  "42": {
    "id": "42", "name": "四级数据库工程师", "grade": 4, "timeMin": 90,
    "parts":   [ { "id": 1, "name": "操作系统原理-单选题", "sections": [...] } ],
    "questions": [ { "id", "kind", "score", "half", "part", "partName",
                     "sec", "secName", "stem", "options", "letters",
                     "refAnswer", "fills", "expl", "images" } ],
    "papers":  [ { "ver", "group", "date", "sections": [{ "ids": [...] }] } ]
  }
}
```

- `kind`：`single` | `multi` | `fill` | `essay`
- `letters`：正确选项字母（多选去重排序）
- `fills`：`[{ n, alts: [...] }]` 每空的备选答案
- 题干里的图片写成 `[[IMG:文件名]]`，由 `RichText` 渲染为 `/images/文件名`

图片在 `server/data/images/`（322 张，含三级 76 + 四级 246）。

## 已知限制

- 四级题库**几乎没有解析**（原库只有 24 条），三级有 1645 条
- 三级有 48 道主观题（设计与应用题）无标准答案，需自评
- 四级 ver 64/65 的 group 11-15 共 10 套卷在原库中只含数据库原理部分
- 单用户本地应用，无鉴权；`progress.json` 直接存盘

## 声明

题库版权归原公司所有。本项目仅供个人学习，请勿分发或商用。
